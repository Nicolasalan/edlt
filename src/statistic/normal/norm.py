#! /usr/bin/env python3

# Autor: Nicolas Alan
import scipy.stats as stats
import math

def norm(mu: float, sigma: float, n: int, var1: float, var2: float) -> float:
     """Example
     * Case 1: P(𝑥̅ ≤ 894)
     * Case 2: P(700 ≤ 𝑥̅ ≤ 894)
     """
     
     return prob

if __name__ == '__main__':
     
     average = 120       # media
     sigma = 16*16          # desvio padrao
     n = 0               # numero de amostras
     var1 = 120          # valor X1
     var2 = 126            # valor X2
     tip = 1             # tipo de calculo

     norm(average, sigma, n, var1, var2, tip)